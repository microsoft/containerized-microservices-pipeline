kind: HorizontalPodAutoscaler
apiVersion: autoscaling/v1
metadata:
  name: login-service-autoscaler
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: login-service-deployment
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 50