# Default values for ingress-routing.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

ingress:
  - path: /login-api
    serviceName: login-service-service
    servicePort: 80
  - serviceName: canary-microservice-app-deployment
    servicePort: 80
